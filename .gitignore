.DS_Store
node_modules/

# Collective DJ layer (Phase 0+): TS build output for the aggregator service.
/aggregator/dist/

# Raw demo footage (large; only the composed clip in docs/img/ is committed)
demo-raw/

# PyInstaller scratch from `just freeze-sidecar` (run from the repo root); the
# bundle output is src-tauri/sidecar-dist (ignored there).
/build/
/slipmate_infer.spec
